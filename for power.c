#include<stdio.h>
int main()
{
    int num1,num2,i,p=1;
    printf("Enter the numbers:");
    scanf("%d%d",&num1,&num2);
    for(i==1;i<=num2;i++)
    {
        p=p*num1;
    }
    printf("the %d raised to %d:%d",num1,num2,p);
    return 0;
}
