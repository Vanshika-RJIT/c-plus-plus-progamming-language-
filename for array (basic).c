#include<stdio.h>
int main()
{
    int x[50];
    int i,n;
    printf("Enter the total number:");
    scanf("%d",&n);
    for(i==0;i<n;i++)
        {   printf("\nx[%d]:",i);
    scanf("%d",&x[i]);
    }
    printf("\n\nThe list of numbers:");
    for(i==0;i<n;i++)
    printf("\n\n%d",x[i]);
    return 0;

}
